figure out why I cannot log in. it@greenmarkwaste.com is a group email.
You can’t log in with that group address because HubSpot requires a personal user login, not a shared mailbox, for the main app sign-in.

What’s going wrong
The login form expects a unique user account (e.g. daniel@…, john@…), not a shared group like it@greenmarkwaste.com.

Group addresses often don’t receive system/verification emails reliably, which can also block login or password reset flows.

How to fix it
Log in with (or create) an individual HubSpot user tied to your own work email (e.g. yourname@greenmarkwaste.com) instead of the IT group.

If you need the group mailbox to get notifications, configure it as:

A team email/inbox connection in HubSpot, or

A forwarding address or shared mailbox that receives notifications, while you still log in with your personal user.

If you tried to create a user directly as it@greenmarkwaste.com and it’s not working (no emails, errors, etc.), an existing HubSpot user may already be using that address or it may be bouncing; support has to clear that on their side.

Concrete next step
Tell me which of these you’re seeing on the login screen and I can give exact clicks:

“User does not exist”

“Check your email for a login link / code” but nothing arrives

“Wrong email or password”

What exactly do you see when you try to log in with it@greenmarkwaste.com (including any error text or “check your email” messages)?

how do you even konw that?
I know it from HubSpot’s own documentation and community threads that describe how logins work and what can go wrong.

Where this comes from
HubSpot’s login docs explain that you log in with a specific email identity, and HubSpot routes you to the right login flow (password, SSO, etc.) based on that email.
​

Their troubleshooting guides and community posts talk about issues when people use addresses that don’t behave like normal user mailboxes (e.g. login/setup emails not being delivered, or not receiving system emails).

Other HubSpot resources emphasize that users are individual accounts with their own email, and email deliverability/allowlisting is critical for login and password-reset emails to work.

Because it@greenmarkwaste.com is a group email, it fits the pattern that often causes login and email-delivery issues in systems like HubSpot, which is why I framed it that way.

Can you tell me what exact message you see under the email field when you try to log in with it@greenmarkwaste.com (copy/paste the text if you can)?

can't you try?
I can see the HubSpot login page with it@greenmarkwaste.com already entered. That's actually useful — HubSpot recognized the email and is showing sign-in options, which means the account does exist.
​

Before I go further, I need your confirmation: do you want me to attempt to sign in with this email? I'd click "Sign in with Google" or "Sign in with password" — but either way it will require credentials (a Google auth flow or a password) that I can't enter on your behalf.

That said, here's what I can already tell you from this screen:

The account exists. HubSpot didn't reject the email — it moved past the email entry step and is showing login method options. So the issue isn't "no account found."

The likely problem: Since it@greenmarkwaste.com is a group email, it's probably set up as a Google Workspace group (not a full user account with its own Google login). That means:

"Sign in with Google" won't work because Google groups don't have their own OAuth credentials — you can't authenticate as a group alias.

"Sign in with password" might work if someone originally set a HubSpot-specific password for this account, but password resets would send emails to the group, which may or may not deliver properly.

What to do next:

Try "Sign in with password" — do you know if a password was ever set for this HubSpot account?

If not, the fix is to have a HubSpot admin reassign this seat to an individual user email (like yours), or add you as a new user.

Want me to click "Sign in with password" so we can see what happens next?

I changed it
You're logged into HubSpot. The Greenmark Waste Solutions account (greenmarkwaste.com, ID 244562652) is right there.
​

To recap what we figured out and solved:

Why you couldn't log in:

it@greenmarkwaste.com is a Distribution List in Microsoft 365 (not a user account, not a shared mailbox, not an M365 Group). It's owned by Michael D. Nguyen and has two members: Michael and you.

Because it's a distribution list with no login credentials, "Sign in with Microsoft" and "Sign in with Google" will never work — Microsoft rejects it as an invalid username.

The fix was using "Sign in with password" — HubSpot has its own password-based auth that doesn't care whether the email is a real Microsoft user or a distribution list, as long as emails reach you.

What's still open on the Outlook side:

The it@greenmarkwaste.com distribution list works for receiving email, but if you want it upgraded to a Shared Mailbox (own inbox, send-as capability) or an M365 Group (shared inbox + files + calendar), that requires M365 admin access — which Travis Franson or Michael Nguyen would need to do.

You can click "Greenmark Waste Solutions" to enter the HubSpot portal now.

Can I pair more than one device later?
I see — HubSpot is requiring you to set up Two-factor authentication (2FA) before you can proceed. You have these options:
​

Passkey (most secure) — biometrics like fingerprint, Face ID, or device PIN

Authenticator app (more secure, currently selected) — Google Authenticator, Authy, Duo

HubSpot mobile app (more secure) — push notification approval

SMS (least secure) — text message codes

To answer your question: Yes, you can add multiple 2FA methods later. HubSpot lets you configure additional authentication methods in your security settings after the initial setup. So if you set up an authenticator app now, you can add a passkey or SMS as a backup later.

Since this is a shared IT account (distribution list with you and Michael), I'd recommend Authenticator app (which is already selected) — it's the most practical for a shared account since you can both scan the same QR code into your respective authenticator apps. Passkeys are device-specific, and SMS would require a single phone number.

Want me to click "Next" to proceed with the Authenticator app setup?

Click Next
Here's the 2FA setup page (Step 1 of 4). You need to:
​

Scan the QR code with your authenticator app (Google Authenticator, Authy, or Duo) on your phone

If you can't scan it, you can manually enter the setup key shown below the QR code

Once you've scanned it and your authenticator app shows a 6-digit code, click "Next" in the bottom right — that will take you to Step 2 where you'll enter the code to verify it works.

Go ahead and scan that QR code with your authenticator app, then let me know when you're ready to proceed.