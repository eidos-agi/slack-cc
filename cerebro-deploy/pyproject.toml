[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cerebro-deploy"
version = "0.1.0"
description = "Rigid step-by-step deployment CLI with verification gates"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
cerebro-deploy = "cerebro_deploy.cli:main"
