# Session plan — 2026-04-16T13:48:24.186601Z

**Scope:** ship query_gold dynamic-dispatch MCP tool + error-as-signal emitter in cerebro-mcp
**Budget:** 4.0h (~238m budget, ~351m hard cap)

## Disciplines
- Phases are sequential — no parallel work across phases.
- Every phase ends with a commit (local for 2-3, pushed in 4-5).
- Kill criteria are real — Phase 2 running long does NOT eat Phase 3's budget.
- Observation before construction — Phase 1 is first, always.
- Phase 5 /land is non-negotiable.

## Out of scope (fill before Phase 1)
_Name 2-3 tempting adjacent threads we are NOT doing this session. Post them in the session log before Phase 1._
- [ ] _TODO: name tempting thread 1 we are NOT doing_
- [ ] _TODO: name tempting thread 2 we are NOT doing_
- [ ] _TODO: name tempting thread 3 we are NOT doing_

## Phases

### Phase 0 — 0-preflight
**Budget:** 17m (hard cap 28m)

- **Goal:** Load contracts, verify services, confirm scope
- **Required output:** confirmed scope + services healthy + credentials loaded
- **Kill criterion:** any blocking service unhealthy or credentials missing

### Phase 1 — 1-observation
**Budget:** 34m (hard cap 51m)

- **Goal:** Verify the assumption driving this session against real data before writing code
- **Required output:** findings document in .research/ with actual numbers, not hypotheses
- **Kill criterion:** observation exceeds 45 min or fails to produce 3+ concrete findings
- **Observation query:**
  ```
  SELECT kind, name, status, COUNT(*) AS n
  FROM events
  WHERE ts > datetime('now', '-7 days')
  GROUP BY 1, 2, 3
  ORDER BY n DESC
  LIMIT 50;
  ```

### Phase 2 — 2-build-a
**Budget:** 68m (hard cap 102m)

- **Goal:** ship query_gold MCP tool — dynamic dispatch over whitelisted *_gold views, unlocks every future gold view without src/index.ts edit
- **Required output:** local commit with passing type-check + test suite
- **Kill criterion:** tests failing after 90 min — git reset on query_gold changes only, continue to Phase 3 with emitter alone
- **Steps:**
  - [ ] Define ALLOWED_VIEWS constant in src/index.ts: sage_gold.entity_pnl, sage_gold.gl_summary, sage_gold.ap_aging
  - [ ] Register query_gold(view: string, filters?: object, limit?: number) via the existing register() helper so it inherits telemetry wrapping
  - [ ] Handler: resolve getAuthProps().jwt → getUserSupabase(SUPABASE_URL, ANON_KEY, jwt) → .schema(schema).from(view).match(filters).limit(limit)
  - [ ] Return shape: { view, rows, row_count, schema } — match existing tool response conventions
  - [ ] Reject views not in ALLOWED_VIEWS with structured error + hint listing allowed names
  - [ ] Add test case in tests/run.sh: query_gold_allowed_view_returns_200, query_gold_unknown_view_returns_400
  - [ ] npm run type-check + npm test → must be green before moving on
- **Gate to advance:** full test suite green (35+ tests) AND manual curl with authed JWT returns real rows from sage_gold.entity_pnl
- **Artifact:** local commit in cerebro-mcp, not yet pushed (bundles with Phase 3 in Phase 4)

### Phase 3 — 3-build-b
**Budget:** 51m (hard cap 68m)

- **Goal:** ship error-as-signal emitter — every tool error also emits kind=unmet_need to cerebro-telemetry with args_hash (NOT raw args, PII risk per rhea)
- **Required output:** local commit with passing type-check + test suite
- **Kill criterion:** tests failing after 60 min — revert emitter changes, ship query_gold alone
- **Steps:**
  - [ ] In src/telemetry.ts withTelemetry(): on status==='error' branch, ALSO emit a second event with kind='unmet_need'
  - [ ] unmet_need.attrs: error_code (category from Phase 1 observation), tool_name, args_hash (SHA256 prefix of JSON.stringify(args), 12 hex chars)
  - [ ] Implementation uses crypto.subtle.digest in the Worker runtime — no extra deps
  - [ ] Preserve existing error response to the caller — this is fire-and-forget dual-emit, not a behavior change
  - [ ] Add test: call a tool that hits a predictable error, assert telemetry stream got a tool_call AND an unmet_need event
  - [ ] npm run type-check + npm test green
- **Gate to advance:** tests green + manual verification: call query_gold with a disallowed view, grep cerebro-telemetry for kind=unmet_need with matching tool_name=query_gold
- **Artifact:** local commit combined with Phase 2 into single atomic deploy unit

### Phase 4 — 4-deploy
**Budget:** 34m (hard cap 51m)

- **Goal:** Ship Phases 2 + 3 as one atomic deploy and verify live
- **Required output:** live /healthz 200 + verification event visible in telemetry
- **Kill criterion:** deploy fails twice — revert to previous, file issue, end with partial work

### Phase 5 — 5-land
**Budget:** 34m (hard cap 51m)

- **Goal:** Capture work in the trilogy and land cleanly
- **Required output:** bookmark written, working trees clean on T1 repos, /land completed
- **Kill criterion:** never — /land is non-negotiable; if exhausted, land minimal
